<?php

    /*
        -> so to handle form we will create controller and route '/user' url to the UserController
        *) Make Html Form
            -> we will create Html Form view inside 'user.blade.php'
        *) Make Controller
            -> we will create a controller on 'UserController.php'
        *) Route Get and Post
            -> WE will route to post and get request on 'web.php'
        *) Get Form Data
            -> we will get the form data back to controller
        *) Uer Validation function
            -> we will apply validation on controller function
        *) Show error message
            -> and we can get the error to 'login.blade.php' if the validation through error
        *) Error with every field
        *) Interview Question
    */
